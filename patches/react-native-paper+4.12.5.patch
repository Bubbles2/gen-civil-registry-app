diff --git a/node_modules/react-native-paper/lib/commonjs/styles/shadow.js b/node_modules/react-native-paper/lib/commonjs/styles/shadow.js
index 24b64aa..1ba7072 100644
--- a/node_modules/react-native-paper/lib/commonjs/styles/shadow.js
+++ b/node_modules/react-native-paper/lib/commonjs/styles/shadow.js
@@ -20,6 +20,10 @@ function shadow() {
   let elevation = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
 
   if (elevation instanceof _reactNative.Animated.Value) {
+    // Android draws depth from elevation; animated iOS shadow styles crash RN 0.73's native driver.
+    if (_reactNative.Platform.OS === 'android') {
+      return {};
+    }
     const inputRange = [0, 1, 2, 3, 8, 24];
     return {
       shadowColor: SHADOW_COLOR,
diff --git a/node_modules/react-native-paper/lib/module/styles/shadow.js b/node_modules/react-native-paper/lib/module/styles/shadow.js
index 07a5d39..d2edea1 100644
--- a/node_modules/react-native-paper/lib/module/styles/shadow.js
+++ b/node_modules/react-native-paper/lib/module/styles/shadow.js
@@ -1,11 +1,15 @@
 import * as Colors from './colors';
-import { Animated } from 'react-native';
+import { Animated, Platform } from 'react-native';
 const SHADOW_COLOR = Colors.black;
 const SHADOW_OPACITY = 0.24;
 export default function shadow() {
   let elevation = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
 
   if (elevation instanceof Animated.Value) {
+    // Android draws depth from elevation; animated iOS shadow styles crash RN 0.73's native driver.
+    if (Platform.OS === 'android') {
+      return {};
+    }
     const inputRange = [0, 1, 2, 3, 8, 24];
     return {
       shadowColor: SHADOW_COLOR,
diff --git a/node_modules/react-native-paper/src/styles/shadow.tsx b/node_modules/react-native-paper/src/styles/shadow.tsx
index fb13085..7894312 100644
--- a/node_modules/react-native-paper/src/styles/shadow.tsx
+++ b/node_modules/react-native-paper/src/styles/shadow.tsx
@@ -1,11 +1,17 @@
 import * as Colors from './colors';
-import { Animated } from 'react-native';
+import { Animated, Platform } from 'react-native';
 
 const SHADOW_COLOR = Colors.black;
 const SHADOW_OPACITY = 0.24;
 
 export default function shadow(elevation: number | Animated.Value = 0) {
   if (elevation instanceof Animated.Value) {
+    // shadow* styles are iOS-only; Android draws depth from `elevation`. RN 0.73
+    // throws when an Animated shadowOffset reaches the native animated module
+    // (FAB.Group animates a Surface with useNativeDriver), so skip them there.
+    if (Platform.OS === 'android') {
+      return {};
+    }
     const inputRange = [0, 1, 2, 3, 8, 24];
 
     return {
